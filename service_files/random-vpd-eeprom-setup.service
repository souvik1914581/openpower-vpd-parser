[Unit]
Description=Service which setups up mock EEPROMs for randomized testing of vpd-manager
Wants=vpd-manager.service
Before=vpd-manager.service

[Service]
ExecStart=/usr/bin/random-eeprom-setup.py
Type=oneshot

[Install]
WantedBy=multi-user.target